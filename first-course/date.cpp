#include <cstdio>
#include <string>


int main(){
    
    char* name[1000];
    int price, number, total{0}, real;
    
    while(scanf("%s\n", name) > 0){
        string name_(name);
        
        if (name == "TOTAL"){
            break
        }

        scanf("%d %d\n", &price, &number);

        total

    }
}