#include <cstdio>
#include <map>
#include <map>
#include <array>
#include <set>

int main(){

    int N, total{0}, left{0}, right{0};
    set<char> s;
    scanf("%d\n", &N);

    char list_chars[N];
    scanf("%[^\n]", list_chars);

    while(right != N){

        if (list_chars[right + 1] != list_chars[right]){
            s.insert(s);
        }
        
    }

}