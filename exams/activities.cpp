#include <cstdio>
#include <algorithm>
#include <unordered_map>
#include <string>

const int NMAX = 3 * 10 * 100 + 1;
char c[NMAX];

int occurences(string target, string sentence){
    int size = target.size();
     
}

int main(){
    
    scnaf("%s", c);
    int n = strlen(c);
    string noun(c);
    bool found = true;

    for (int i = 0; i < n; ++i){
        char in = noun[i];
    
        for (int i = i; i < n; ++i){
             
        }   
    
    }

}   